{
  "name": "stillresonating.com",
  "scripts": {
    "build": "webpack && eleventy",
    "serve": "eleventy --serve & webpack -w ; wait",
    "deploy": "GIT_SHORT_HASH=$(git rev-parse --short=8 HEAD) SITE_BASE_URL=stillresonating.com npm run build && gh-pages -d _site",
    "lint": "eslint assets",
    "format": "prettier \"**/*.{css,ts,json,html,md}\" --write"
  },
  "license": "MIT",
  "homepage": "https://github.com/johnteskemusic/stillresonating.com#readme",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-img": "^0.8.2",
    "eleventy-lib": "git+https://github.com/johnteske/eleventy-lib.git#main",
    "gh-pages": "^3.1.0",
    "html-minifier": "^4.0.0",
    "prettier": "^2.2.1",
    "ts-loader": "^8.0.12",
    "typescript": "^4.1.3",
    "webpack": "^5.11.0",
    "webpack-cli": "^4.2.0"
  }
}
